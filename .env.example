# Proxmox Connection Settings
# ============================
# Proxmox API endpoint (required)
PROXMOX_HOST=https://px.url.local

# Authentication Method 1: API Token (recommended)
# Create token in Proxmox: Datacenter > API Tokens > Add
PROXMOX_TOKEN_ID=root@pam!abc123
PROXMOX_TOKEN_SECRET=abc123

# Authentication Method 2: Username/Password (less secure)
# PROXMOX_USER=user@pve
# PROXMOX_PASSWORD=your-password

# Tailscale Configuration
# =======================
# Get auth key from: https://login.tailscale.com/admin/settings/keys
# if tailscale_api_key is provided auth keys are auto generated
#TAILSCALE_AUTH_KEY=tskey-auth-123

# Tailscale API configuration for node management
# Get API key from: https://login.tailscale.com/admin/settings/keys
TAILSCALE_API_KEY=tskey-api-xxxxx
# Your tailnet organization (e.g., your-org.ts.net or email domain)
TAILSCALE_TAILNET=your-org.ts.net

# SSH Settings for Provisioning
# ==============================
SSH_KEY_PATH=~/.ssh/id_ed25519
SSH_USER=root
SSH_TIMEOUT=30

# Container Defaults
# ==================
DEFAULT_CORES=2
DEFAULT_MEMORY=1024
DEFAULT_STORAGE=10
DEFAULT_BRIDGE=vmbr0
DEFAULT_STORAGE_POOL=nvmeu2-vmstore

# Template Storage
# ================
# Storage name where LXC templates are stored (e.g., local, cephfs, nfs)
TEMPLATE_STORAGE=c137-isos

# Security Note
# =============
# This file contains sensitive credentials. Never commit it to version control.
# The .env file is gitignored by default. Keep your credentials safe!

# Application Settings
# ====================

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Output format (table, json, yaml)
DEFAULT_OUTPUT_FORMAT=table

# Enable colored output
COLORED_OUTPUT=true

# Dry run mode (validate without creating)
DRY_RUN=false

# Development Settings
# ====================

# Enable debug mode
DEBUG=false

# Mock Proxmox API (for testing)
MOCK_PROXMOX=false