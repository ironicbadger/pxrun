# Proxmox Connection Settings
# ============================

# Proxmox API endpoint (required)
PROXMOX_HOST=https://proxmox.example.com:8006

# Authentication Method 1: API Token (recommended)
# Create token in Proxmox: Datacenter > API Tokens > Add
PROXMOX_TOKEN_ID=user@pve!pxrun
PROXMOX_TOKEN_SECRET=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Authentication Method 2: Username/Password (less secure)
# PROXMOX_USER=user@pve
# PROXMOX_PASSWORD=your-password

# SSH Settings for Provisioning
# ==============================

# SSH key path for connecting to Proxmox nodes
# If not set, will use default SSH key (~/.ssh/id_rsa)
SSH_KEY_PATH=~/.ssh/proxmox_key

# Default SSH user for Proxmox nodes (usually root)
SSH_USER=root

# SSH timeout in seconds
SSH_TIMEOUT=30

# Container Defaults
# ==================

# Default values for container creation
DEFAULT_CORES=2
DEFAULT_MEMORY=1024
DEFAULT_STORAGE=10
DEFAULT_BRIDGE=vmbr0
DEFAULT_STORAGE_POOL=local-lvm

# Provisioning Settings
# =====================

# Tailscale auth key for automatic VPN setup
# Get from: https://login.tailscale.com/admin/settings/keys
TAILSCALE_AUTH_KEY=tskey-auth-XXXXXXXXXXXXXXXXX

# Docker installation options
# Note: docker compose is now integrated into Docker itself (not a separate binary)

# Security Note
# =============
# This file contains sensitive credentials. Never commit it to version control.
# The .env file is gitignored by default. Keep your credentials safe!

# Application Settings
# ====================

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Output format (table, json, yaml)
DEFAULT_OUTPUT_FORMAT=table

# Enable colored output
COLORED_OUTPUT=true

# Dry run mode (validate without creating)
DRY_RUN=false

# Development Settings
# ====================

# Enable debug mode
DEBUG=false

# Mock Proxmox API (for testing)
MOCK_PROXMOX=false